[pytest]
# The suite lives in tests/. Without this, pytest also collects root-level
# scripts whose name starts with test_ - and test_agent.py exits(1) on import
# when MINIMAX_API_KEY is unset, which turns a developer smoke script into an
# INTERNALERROR for the whole run (found by CI on 2026-09-23).
testpaths = tests
