# 隐私说明

Bobodan（波波蛋）是本地优先的个人学习助手。本文档说明你的资料和数据在应用运行时的流向、存储位置与删除边界。

## 一句话总结

**你的原始资料、知识图谱、个人知识与学习记录默认只保存在你自己的电脑上。** 只有当你配置了云端模型服务、联网研究或向量服务时，相关内容才会按你选择的服务商条款发送到外部。

## 数据分类与存储位置

| 数据 | 存储位置 | 说明 |
|---|---|---|
| 原始资料（Markdown / PDF / DOCX / PPTX） | 你选择的资料库文件夹（`raw/` 等目录） | 应用只读取，AI 不会自动修改；删除时进入归档目录 |
| RAG 索引与检索记录 | 资料库 `.bobodan/knowledge.db`（或 `.knowledge/`） | 从资料重建，可删除后重建 |
| 知识图谱（概念、关系、证据） | 资料库 `.bobodan/concept_graph.db` | 只包含你审查确认的内容 |
| 个人知识、学习事件、候选 | `personal-knowledge.db` + 资料库 `bobodan.db` | 长期学习真相源 |
| 会话历史 | `.session/` | 本地会话记录 |
| 偏好设置 | `%APPDATA%\Bobodan`（桌面版） | 用户级配置 |
| 用量与成本账本 | `usage.db` | 只记请求数、Token 与模型，不记提示词正文 |
| 网页证据快照 | 资料库 `research.db` | 你确认后保存的网页快照 |

## 数据流向

### 本地处理（默认，不联网）

导入、解析、切块、FTS5 中文检索、概念提取、练习生成、批改、复习调度全部在本地进程完成。关闭网络后应用的核心学习闭环仍然可用。

### 云端模型服务（按你的配置）

- 当你配置 DeepSeek / MiniMax / OpenAI / 通义 / 硅基流动 / OpenRouter 等 Provider 时，**对话内容、检索到的资料片段、练习题与批改请求**会发送到对应服务商处理。请查阅各服务商的数据政策。
- 应用本身不收集、不上传你的资料到 Anthropic、Bobodan 或任何第一方服务器——本应用没有自有云端。

### 联网研究（需你确认）

- 本地资料不足时，模型只能**提议**联网；你确认后才会调用 Tavily / Exa 搜索，选择来源后读取网页正文。
- 搜索摘要不进入回答证据；已读取的正文以不可变快照保存在本地 `research.db`。
- 直接读取失败时可使用 Jina Reader 后备（请求内容同样发往 Jina）。

### 向量检索（可选）

- 使用 Qdrant 时，嵌入向量默认保存在本地；若你配置远程 Qdrant 服务，索引内容会发送到该服务。
- Ollama 本地模型在你自己机器上运行，数据不出本机。

## 删除与导出

- 资料库、会话、个人知识、图谱与旧记忆迁移均可在应用内删除或导出。
- 导出文件只作为快照，不反向参与运行时检索。
- 卸载应用时，你的资料库默认保留在你选择的文件夹；卸载器行为契约见安装说明（P5G.2）。

## 更新与审计

本文档与 `THIRD_PARTY_NOTICES.md` 随每次发布更新。涉及数据流向的变更必须先更新本文档，再发布。
