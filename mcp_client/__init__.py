"""Bobodan MCP client integration.

Local package name is `mcp_client` to avoid colliding with the
official `mcp` Python SDK (from modelcontextprotocol). All code that
needs the SDK should `import mcp` to get the official package.
"""

__version__ = "0.1.0"
